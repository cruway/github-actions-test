name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Test github action"
      - name: multiline script
        run: |
          node -v
          npm -v
      - name: python Command
        run: |
          import ploatform
          print(platform.processor())
        shell: python